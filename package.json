{
  "name": "@jobs-it",
  "private": true,
  "repository": {
    "url": "git@github.com:el-frontend/job-it-challenge.git"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "classnames": "^2.5.1"
  }
}
